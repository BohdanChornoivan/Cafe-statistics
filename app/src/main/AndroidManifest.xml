<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.scoliztur.creative">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
    <activity android:name=".ui.ShowMainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ShowBalance"
            android:label="ShowGoods">
        </activity>
        <activity
            android:name=".ui.ShowIngredients"
            android:label="ShowIngredients">
        </activity>
        <activity
            android:name=".ui.ShowGoods"
            android:label="ShowGoods">
        </activity>
        <activity
            android:name=".ui.ShowCosts"
            android:label="ShowCosts">
        </activity>
    </application>

</manifest>